module.exports = {
    port: 8080,
    session: {
        resave: true,
        saveUninitialized: true,
        secret: "your secret",
        unset: "destroy",
        cookie: {
            maxAge: 86400000,
        }
    },
    servers: {
        "banana": {
            host: "example.com",
            port: 22
        }
    }
};
